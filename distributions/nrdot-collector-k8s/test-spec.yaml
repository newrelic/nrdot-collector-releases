whereClause:
  host:
    template: "WHERE k8s.cluster.name='{{ .clusterName }}'"
    vars:
      - clusterName
slow:
  testCaseSpecs:
    - k8s
    - host
nightly:
  collectorChart:
    name: newrelic/nr-k8s-otel-collector
    version: 0.8.28
  ec2:
    enabled: false
  testCaseSpecs:
    - k8s
    - host