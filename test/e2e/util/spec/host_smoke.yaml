whereClause:
  template: "WHERE testKey like '{{ .testKey }}'"
  vars:
    - testKey

testCases:
  host_receiver.any_system_metric:
    metric:
      name: system.%
    assertions:
      - aggregationFunction: count
        comparisonOperator: ">"
        threshold: 1